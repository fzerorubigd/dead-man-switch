name: I am alive check Mostodon
on: 
  workflow_dispatch:
  schedule:
  - cron: "0 2 * * *"

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - run: |
          go run ./signal/mostodon